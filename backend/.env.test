# Configuration Base de Données de Test
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=nourx_test
POSTGRES_PASSWORD=test_password
POSTGRES_DB=nourx_app_test

# Configuration Serveur
NODE_ENV=test
PORT=3002
HOST=localhost

# Configuration Better Auth (test)
BETTER_AUTH_SECRET=test-secret-key-32-characters-long
BETTER_AUTH_URL=http://localhost:3002

# Configuration JWT (test)
JWT_SECRET=test-jwt-secret-key
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=1d

# Configuration Email (mock)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=test
SMTP_PASSWORD=test
SMTP_FROM_NAME="Nourx Test"
SMTP_FROM_EMAIL=test@nourx.com

# Configuration CORS
CORS_ORIGIN=http://localhost:3000

# Configuration Logs
LOG_LEVEL=error

# Configuration Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_AUTH_MAX=10

# Configuration Upload
MAX_FILE_SIZE=1048576
UPLOAD_PATH=./storage/test-uploads

# Configuration Admin par défaut (test)
DEFAULT_ADMIN_EMAIL=admin@test.com
DEFAULT_ADMIN_PASSWORD=TestPassword123!
DEFAULT_ADMIN_NAME="Test Admin"